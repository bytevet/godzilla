# Expected findings for this sample (see test/README.md). Regenerate with:
#   GODZILLA_REGEN=1 go test ./test/corpus/ -run RegenerateManifests
# Guards inter-procedural taint through a bare-named local helper (arg->param
# and return-taint), previously dropped due to a callee-name mismatch.
findings:
    - rule: js-sqli
      min: 1
