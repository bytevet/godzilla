# Expected findings for this sample (see test/README.md). Regenerate with:
#   GODZILLA_REGEN=1 go test ./test/corpus/ -run RegenerateManifests
# Exercises the path.join propagator edge (taint reaches fs.readFile only
# through the propagator, not by direct use).
findings:
    - rule: js-path-traversal
      min: 1
