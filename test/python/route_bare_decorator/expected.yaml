# Expected findings for this sample (see test/README.md). Regenerate with:
#   GODZILLA_REGEN=1 go test ./test/corpus/ -run RegenerateManifests
# Only Api.parse_urls is a route (bare @post + @permission), so only its `url`
# param is seeded. Helper.fetch carries the same bare @post with no
# access-control decorator and must stay clean — max pins that.
findings:
    - rule: py-ssrf
      min: 1
      max: 1
      line: 23
      sink: "py:requests.get"
