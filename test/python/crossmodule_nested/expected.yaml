# Expected findings for this sample (see test/README.md). Regenerate with:
#   GODZILLA_REGEN=1 go test ./test/corpus/ -run RegenerateManifests
# The sink is in the callee, one package deep, reached through a cross-module
# call whose import path is shorter than the module's scan-root-relative path.
# Resolution must be independent of where the scan root sits.
findings:
    - rule: py-ssrf
      min: 1
      max: 1
      line: 7
      sink: "py:requests.get"
