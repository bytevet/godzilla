# Expected findings for this sample (see test/README.md). Regenerate with:
#   GODZILLA_REGEN=1 go test ./test/corpus/ -run RegenerateManifests
# A tainted template SOURCE is genuinely both SSTI (compiled+executed) and
# reflected XSS (rendered into the response), so both rules fire.
findings:
    - rule: py-ssti
      min: 1
    - rule: py-xss
      min: 1
