# Expected findings for this sample (see test/README.md). Regenerate with:
#   GODZILLA_REGEN=1 go test ./test/corpus/ -run RegenerateManifests
# Negative control: a value-struct param on a NON-route-handler is not a bound
# form, so nothing is seeded and no finding is expected.
findings: []
