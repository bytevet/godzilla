# Expected findings for this sample (see test/README.md). Regenerate with:
#   GODZILLA_REGEN=1 go test ./test/corpus/ -run RegenerateManifests
# The line/sink assertions (TRUST-3) upgrade the oracle from "rule fired" to
# "rule fired at the right sink", catching a wrong-location finding.
findings:
    - rule: go-sql-injection
      min: 2
      line: 62
      sink: Query
