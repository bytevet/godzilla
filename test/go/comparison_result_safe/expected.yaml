# Only the CONCATENATION flow may fire; the equality test on the same tainted
# value reaches exec.Command too, but a boolean cannot carry an injection.
findings:
    - rule: go-command-injection
      min: 1
      max: 1
      line: 21
      sink: Command
