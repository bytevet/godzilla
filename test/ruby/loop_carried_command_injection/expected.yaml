# The tainted request param reaches system() only via the loop's back-edge
# (header PHI), which the real CFG models and the old single-block lowering did
# not — so this fires only after the Phase 1b SSA/CFG adoption.
findings:
  - rule: ruby-command-injection
    min: 1
